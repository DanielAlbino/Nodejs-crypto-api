const getSpread = (req, res) => {
  res.status(200).send({ message: "success" });
};

const updateSpread = (req, res) => {
  res.status(200).send({ message: "success" });
};

const addNewSpread = (req, res) => {
  res.status(200).send({ message: "success" });
};

const deleteSpread = (req, res) => {
  res.status(200).send({ message: "success" });
};

module.exports = {
  getSpread,
  updateSpread,
  addNewSpread,
  deleteSpread,
};
